# Domain Docs

Single-context layout.

## Consumer rules

- `CONTEXT.md` at repo root — project terminology and domain language
- `docs/adr/` at repo root — architectural decision records

Skills like `improve-codebase-architecture`, `diagnose`, `tdd`, and `zoom-out` read these files to understand the project context.
