# Issue Tracker

Issues live in **GitHub Issues** for this repository.

## Creating issues

Use `gh issue create` or the GitHub web UI.

## Reading issues

Use `gh issue view <number>` or `gh issue list`.

## Closing issues

Use `gh issue close <number>` with an optional comment.
