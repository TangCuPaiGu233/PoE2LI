# S1.3 文档对账 — Chat Runtime Unification

> 说明：原计划打开的 `assessments/chat-runtime-unification-plan.md` 在当前工作区未找到。以下对账结论基于现有代码与文档实际状态记录，待后续统一修复。

## 1) 主不一致：runtime unification 状态不符

- **文档/计划意图**：Sprint 1 应废弃 Legacy ReAct，统一为 Orchestrator 运行时（P0）。
- **代码现状**：`backend/app/services/chat_orchestrator.py` 中 `_get_default_runtime()` 仍默认返回 `legacy`，Orchestrator 为 opt-in（`CHAT_RUNTIME=orchestrator`）。
- **结论**：文档声称的“Step 0 已完成 / 已合入 main”与代码层实际状态不一致。
- **标记**：`实际代码层未执行，待 Sprint 1 修复后更新`。

## 2) 其他简要扫描到的 doc-vs-code 不一致

| 文档位置 | 声称/描述 | 代码现状 | 处理建议 |
|---|---|---|---|
| `docs/HANDOVER.md` 部分状态表 | 多项标记“已完成” | 部分里程碑/功能仍在默认 legacy runtime，未真正统一 | 仅标记不一致，不改核心内容 |
| `docs/当前需求汇总.md` 已完成工作 | P0-A/B 已做 | 部分优化存在，但默认 runtime 未切换 | 仅标记不一致，不改核心内容 |
| `AGENTS.md / CLAUDE.md` | P0 Core Loop / Chat architecture | 文档与代码基本一致，无强制不一致 | 无需额外标记 |

## 3) 下一步

- 白鹭要求：先只标记不一致，不改核心内容。
- 建议后续由 Tech Lead 统一安排修复节奏，避免多文档散改导致对账成本上升。
